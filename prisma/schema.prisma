generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb" // If you're using MongoDB
  url      = env("DATABASE_URL")
}

model Badge {
  id             String   @id @default(auto()) @map("_id") @db.ObjectId
  storeId        String
  selectedBadges String[]
}
